"use server";

import { pool } from "../../_lib/db";
import { getUserId } from "../../_utils/authUtils";

// === FÖRHANDSGRANSKNING ACTIONS ===
// Framtida funktioner för PDF/Email-hantering kommer här

// Exempel:
// export async function generatePDF(fakturaId: number) { ... }
// export async function sendEmail(fakturaId: number, email: string) { ... }
